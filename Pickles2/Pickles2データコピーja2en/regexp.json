// jsonのような記述で、シングルクォートで括ることも、json内にコメントを書くことも可能です。
{
  // 1.mkdir コピー先のディレクトリを作成
  "/止めに対してindexを追加"  :{"mg": ['^(.*)\/$', '$1/index.html']},
  "mkdir "  :{"mg": ['(.*\/*)\/(.*)', 'mkdir .$1']},
  "文章全体を関数化"         :{"m": ['((.*\n*.*)*)','#!/bin/sh\n#Openをクリックして1〜3を順に行って下さい。\n$1']}

  // 2.htmlファイルのコピー
  // "/止めに対してindexを追加"  :{"mg": ['^(.*)\/$', '$1/index.html']},
  // "cpコマンド化"  :{"mg": ['^(.*)$', 'cp ./jp$1 .$1']},

  // 3.gui関連ファイルのコピー
  // "/止めに対してindexを追加"  :{"mg": ['^(.*)\/$', '$1index.html']},
  // ".htmlを_files/に置換"  :{"mg": ['^(.*).html$', '$1_files']},
  // "cpコマンド化"  :{"mg": ['^(.*)$', 'cp ./jp$1 .$1']}
}